# pythondata 项目说明

基于 **领星 OpenAPI + MySQL + 飞书** 的数据采集、同步与分析管道。项目用于把领星业务数据同步到 MySQL，并结合飞书多维表的业务填报，生成销量预估、运营预计下单、系统预测对比、采购建议、库存预估和面料用量预估。

> 重要说明：费用单创建流程已下线。本项目不再创建、作废、上传任何领星费用单；仍保留利润报表同步与计算字段更新，用于经营分析和后续数据看板。

---

## 一、项目结构总览

```text
pythondata/
├── .env                          # 环境配置，需自行创建
├── requirements.txt               # Python 依赖，Python >= 3.8
├── common/                        # 公共模块：配置、数据库、飞书、日志
├── lingxing/                      # 领星 OpenAPI Python SDK
├── utils/                         # 日期、数据转换工具
├── jobs/
│   ├── Sync_data/                 # 领星数据同步：Listing、利润报表等
│   ├── feishu/                    # 飞书读写、销量预估、下单对比、面料预估、采购建议
│   └── purchase_analysis/         # 产品、FBA、库存、销量、采购、库存预估
└── scripts/                       # 部署、维护、定时任务入口脚本
```

---

## 二、核心业务流程

### 1. 利润报表同步

入口脚本：`scripts/run_profit_report_fee_sync.sh`

执行内容：

1. `jobs.Sync_data.fetch_profit_report_msku_daily`：从领星拉取利润报表 MSKU 按日数据；
2. `jobs.Sync_data.update_profit_report_calculated_fields`：更新库内计算字段。

说明：该脚本文件名保留了历史命名中的 `fee`，是为了兼容服务器已有定时任务路径；实际逻辑已不再创建费用单。

### 2. 数据同步与面料预估

入口脚本：`scripts/run_data_sync.sh`

执行内容：

1. 拉取 Listing；
2. 拉取飞书定制面料参数；
3. 拉取飞书面料数据；
4. 生成下单对比表；
5. 生成面料预估表。

### 3. 采购分析

入口脚本：`scripts/run_jobs.sh`

执行内容：`jobs.purchase_analysis.main`

主流程包括：产品管理增量、FBA 库存、仓库库存明细、销量统计 MSKU 月度、采购单、库存预估表。

### 4. 库存同步

入口脚本：`scripts/run_inventory_sync.sh`

执行内容：

1. 采集仓库库存明细；
2. 写入飞书库存多维表；
3. 获取货件单号并写入飞书。

### 5. 采购建议流水线

入口脚本：`scripts/run_procurement_pipeline.sh`

执行内容：

1. `write_order_forecast_to_feishu`：从飞书读取运营预计下单量并写入 MySQL；
2. `generate_forecast_comparison`：生成系统预测 vs 运营预计对比表；
3. `generate_procurement_report`：生成建议下单量和面料用量报告；
4. `export_procurement_excel`：导出采购建议 Excel。

---

## 三、主要模块说明

### common

| 文件 | 作用 |
|---|---|
| `config.py` | 从 `.env` 读取环境、领星、数据库、飞书等配置。 |
| `database.py` | MySQL 连接和游标封装。 |
| `feishu.py` | 飞书 OpenAPI / 多维表读写封装。 |
| `logger.py` | 日志封装。 |

### jobs/Sync_data

| 文件 | 作用 |
|---|---|
| `fetch_listing.py` | 从领星拉取亚马逊 Listing 并入库。 |
| `fetch_profit_report_msku_daily.py` | 拉取利润报表 MSKU 按日数据。 |
| `update_profit_report_calculated_fields.py` | 更新利润报表计算字段。 |
| `diagnose_query_performance.py` | 利润报表相关 SQL 性能诊断。 |

### jobs/feishu

| 文件 | 作用 |
|---|---|
| `write_sales_to_feishu.py` | 从数据库生成/更新飞书销量预估表。 |
| `write_order_forecast_to_feishu.py` | 从飞书读取运营预计下单量并写入数据库。 |
| `forecast_sales_improved.py` | 系统销量预测算法。 |
| `generate_forecast_comparison.py` | 生成系统预测 vs 运营预计对比表，含 SPU 和 SKU 维度。 |
| `generate_order_comparison.py` | 采购单 + 运营预计下单生成下单对比。 |
| `generate_fabric_forecast.py` | 生成面料预估表，支持总量/带颜色、系统/运营双口径。 |
| `generate_procurement_report.py` | 生成采购建议和面料预计用量。 |
| `export_procurement_excel.py` | 导出采购建议 Excel 报告。 |
| `fetch_fabric_params.py` | 飞书定制面料参数入库。 |
| `fetch_feishu_data.py` | 飞书面料表入库。 |
| `write_inventory_to_feishu.py` | 仓库库存明细写飞书。 |
| `Shipment_Number.py` | 领星货件信息写飞书。 |
| `update_product_images.py` | Listing 图片回写飞书。 |

### jobs/purchase_analysis

| 文件 | 作用 |
|---|---|
| `main.py` | 采购分析主入口。 |
| `fetch_product.py` | 领星产品管理数据。 |
| `fetch_fba_inventory.py` | 领星 FBA 库存。 |
| `fetch_inventory_details.py` | 领星仓库库存明细。 |
| `fetch_sale_stat_v2_msku_monthly.py` | 领星销量统计 MSKU 月度。 |
| `fetch_purchase.py` | 领星采购单。 |
| `generate_inventory_estimate.py` | 生成库存预估表。 |
| `shop_mapping.py` | 店铺 ID 与名称映射。 |

---

## 四、启动命令

以下默认项目部署在 `/opt/apps/pythondata`。

| 场景 | 命令 |
|---|---|
| 利润报表同步 + 计算字段更新 | `cd /opt/apps/pythondata && bash scripts/run_profit_report_fee_sync.sh` |
| 数据同步 + 面料预估 | `cd /opt/apps/pythondata && bash scripts/run_data_sync.sh` |
| 采购分析 | `cd /opt/apps/pythondata && bash scripts/run_jobs.sh` |
| 库存同步 | `cd /opt/apps/pythondata && bash scripts/run_inventory_sync.sh` |
| 采购建议流水线 | `cd /opt/apps/pythondata && bash scripts/run_procurement_pipeline.sh` |

---

## 五、服务器定时任务建议

推荐在服务器 `/etc/cron.daily/` 中放置一个无后缀可执行入口，例如 `/etc/cron.daily/pythondata-daily`：

```bash
#!/bin/bash
PROJECT_DIR="/opt/apps/pythondata"
"$PROJECT_DIR/scripts/run_profit_report_fee_sync.sh" || true
"$PROJECT_DIR/scripts/run_data_sync.sh" || true
"$PROJECT_DIR/scripts/run_jobs.sh" || true
"$PROJECT_DIR/scripts/run_inventory_sync.sh" || true
"$PROJECT_DIR/scripts/run_procurement_pipeline.sh" || true
```

实际生产环境请以服务器当前 crontab、`/etc/cron.daily/`、`/etc/cron.d/` 和脚本内容为准。

---

## 六、环境与维护

- Python：>= 3.8
- 依赖：见 `requirements.txt`
- 配置：项目根目录 `.env`，参考 `common/config.py`
- 日志：项目下 `logs/`
- 维护脚本：
  - `scripts/setup_server.sh`
  - `scripts/update_project.sh`
  - `scripts/update_project_force.sh`
  - `scripts/check_status.sh`
  - `scripts/check_venv.sh`

---

## 七、当前状态

当前项目已经打通：

```text
领星数据 → MySQL → 飞书销量预估表 → 运营填写预计下单 → 回写 MySQL
      → 系统预测/运营预计对比 → 采购建议 → 面料预估 → Excel 报告
```

费用单相关能力已移除，后续不再作为本项目流程的一部分。